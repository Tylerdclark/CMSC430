-- Discussion Program

function drinkingAgeCalc age: integer returns boolean;
    canDrink: boolean is false;

begin
    if age >= 21 then
        not canDrink;
    else
        canDrink;
    endif;
end;
